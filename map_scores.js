import { BreakGraph, ScoreInfo } from 'pixel_combats/room';
import { addTeamScores } from './team_scores.js';

// Твои настройки очков за действия
const ENEMY_BLOCK_SCORE = 25;
const MAP_BLOCK_SCORE = 1;
const PLACE_BLOCK_SCORE = 1;

// Здесь размещается логика applyMapEditScores для фиксации правок
export function applyMapEditScores(details, blueTeam, redTeam) {
    // Твой код обработки изменений карты
}
